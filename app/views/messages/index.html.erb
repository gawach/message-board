<h1>メッセージ一覧</h1>

<ul>
  <% @messages.each do |message| %>
    <li><%= link_to message.id, message %> : <%= message.content %> &gt; <%= message.content %></li>
  <% end %>
</ul>
<%= paginate @messages %>

<%= link_to '新規投稿メッセージの投稿', new_message_path %>